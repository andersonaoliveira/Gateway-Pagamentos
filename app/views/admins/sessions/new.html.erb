<div class="ls-login-parent">
  <div class="ls-login-inner">
    <div class="ls-login-container">
      <div class="ls-login-box">
        <h1 class="ls-login-logo">
          <img title="Logo Login" src="https://s3-sa-east-1.amazonaws.com/prod-jobsite-files.kenoby.com/uploads/queroserdev21-1631540701-logo-qsdpng.png">
        </h1>
        <%= form_for(resource, as: resource_name, url: session_path(resource_name), class: "ls-form ls-login-form") do |f| %>
          <fieldset>
            <label class="ls-label">
              <b class="ls-label-text ls-hidden-accessible">E-mail</b>
              <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"ls-field-lg", placeholder: "Email" %>
            </label>
            <label class="ls-label">
              <b class="ls-label-text ls-hidden-accessible">Senha</b>
              <%= f.password_field :password, autocomplete: "current-password", class: "ls-field-lg", placeholder: "Senha" %>
            </label>
            <% if devise_mapping.rememberable? %>
              <div class="field">
                <%= f.check_box :remember_me %>
                <%= f.label :remember_me %>
              </div>
            <% end %>
            <%= f.submit "Entrar", class: "ls-btn-primary ls-btn-block ls-btn-lg" %>
          </fieldset>
        <% end %>
        <% if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
        <div class="field">
          <%= link_to "Esqueceu a senha?", new_password_path(resource_name), style: "color: grey" %>
        </div>
          <% end %>
        <hr>
        <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
          <div class="ls-btn-primary ls-btn-block ls-btn-lg">
            <%= link_to "Inscrever-se", new_registration_path(resource_name), style: "color: white" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

